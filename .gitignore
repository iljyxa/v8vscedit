# Скрытые папки и служебные файлы
.cursor/
.vscode/
.codex
.v8vscedit/
.kilo
env.json

# Зависимости и артефакты сборки
node_modules/
dist/
out/
*.vsix
coverage/
coverage-*

# Примеры конфигураций (только для справки, не для VCS)
example2/
example/

# Логи и временные файлы
*.log
npm-debug.log*

.vscode-test/
.DS_Store

docs/1cdocs

# Личные настройки разработчика (у каждого свои). Остальное в .claude/ — общее:
# агенты (.claude/agents/), команды (.claude/commands/), общие настройки (.claude/settings.json)
# — коммитятся и шарятся с командой.
.claude/settings.local.json
