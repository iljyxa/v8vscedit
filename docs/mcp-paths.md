# Канон путей и типов 1С для MCP-инструментов v8vscedit

Документ — единый и обязательный справочник правил именования путей метаданных
и типов 1С, которые принимают и возвращают MCP-инструменты расширения
`v8vscedit`. Целевая аудитория — ИИ-агенты, подключённые к локальному
MCP-серверу расширения, и разработчики этих агентов.

Канон один: только русские формы, без английских алиасов, без legacy-форм
7.7/8.0, с предсказуемой структурой. Любой инструмент, который сейчас принимает
аргумент `path`, ожидает строку строго в этом формате. Любой инструмент,
который возвращает путь к узлу, выдаёт его в том же формате.

Документ соответствует главам 1-2 плана `../MCP_REFACTORING_PLAN.md` и
является обязательным дополнением к разделу «MCP-инструменты модификации
конфигурации» в `../AGENTS.md`.

---

## 1. Главные правила

### 1.1. Множественное число для корней-коллекций

Корневой сегмент пути для коллекций метаданных — всегда множественное число:
`Справочники`, `Документы`, `РегистрыСведений`, `Перечисления`, `Отчеты`,
`Обработки`, `Роли`. Единственное число (`Справочник.Контрагенты`) — это форма
ссылки в запросах 1С, а не путь метаданных, и MCP такие пути отвергает.

### 1.2. Единственное число для подсистем и псевдо-корней

Подсистемы — единственное число: `Подсистема.Продажи`. Псевдо-корни, у которых
по определению ровно один экземпляр в конфигурации, тоже единственного числа:
`Конфигурация`, `Расширение`.

### 1.3. CamelCase без пробелов

Все сегменты — CamelCase без пробелов, дефисов и подчёркиваний:
`КурсыВалют`, не `курсы валют`, не `КУРСЫ_ВАЛЮТ`. Имена объектов на диске
обычно уже соответствуют этому правилу, потому что 1С EDT не пускает пробелы
в именах метаданных.

### 1.4. Точка — единственный разделитель сегментов

Разделитель сегментов — точка. Точка внутри имени сегмента запрещена:
платформа 1С это правило соблюдает на уровне валидатора имён метаданных,
поэтому резолвер инструментария считает любую точку признаком разделителя.

### 1.5. Реквизиты и табличные части — прямые сегменты

Реквизит и табличная часть присоединяются к корневому объекту напрямую,
без сегмента-роли: `Справочники.Контрагенты.ИНН` (реквизит),
`Справочники.Контрагенты.КонтактныеЛица` (табличная часть). Имена в этих
двух категориях уникальны внутри объекта по правилам платформы — конфликт
разрешения невозможен.

### 1.6. Внутри табличной части сегменты возвращаются

Стоит спуститься на уровень ниже — внутрь табличной части — и сегменты
возвращаются. Колонки идут как реквизиты табличной части:
`Справочники.Контрагенты.ТабличнаяЧасть.КонтактныеЛица.Реквизит.ФИО`.
Это нужно, потому что внутри ТЧ могут быть как реквизиты, так и стандартные
реквизиты с одинаковыми именами.

Тот же контейнерный паттерн — второй прецедент у HTTP-сервисов: `URLTemplate`
(URL-шаблон) — контейнер для `Method` (метод), и путь возвращает сегмент
`Метод` так же, как ТЧ возвращает `Реквизит`:
`HTTPСервисы.X.URLШаблон.T.Метод.M`. В отличие от ТЧ, сам `URLШаблон` —
не прямой сегмент (см. §2.4): `HTTPСервисы.X` не содержит других
одноимённых по возможности коллекций, но URL-шаблон синтаксически
рассматривается как отдельный тип метаданных (`MetaKind`), а не роль внутри
`HTTPService`, поэтому его путь всегда идёт через сегмент `URLШаблон`,
без прямой формы.

### 1.7. Подсистемы — без удвоения сегмента

Иерархия подсистем строится без повтора корневого сегмента:
`Подсистема.Продажи.Розница.Возвраты`, а не
`Подсистема.Продажи.Подсистема.Розница.Подсистема.Возвраты`. Префикс
`Подсистема` присутствует только один раз — в самом начале пути.

---

## 2. Полные таблицы канонических путей

### 2.1. Корни — коллекции метаданных

| Корневой сегмент | Тип метаданных | XML-папка |
|---|---|---|
| `Подсистема` | Subsystem | `Subsystems` |
| `ОбщиеМодули` | CommonModule | `CommonModules` |
| `ПараметрыСеанса` | SessionParameter | `SessionParameters` |
| `Роли` | Role | `Roles` |
| `ОбщиеРеквизиты` | CommonAttribute | `CommonAttributes` |
| `ПланыОбмена` | ExchangePlan | `ExchangePlans` |
| `КритерииОтбора` | FilterCriterion | `FilterCriteria` |
| `ПодпискиНаСобытия` | EventSubscription | `EventSubscriptions` |
| `РегламентныеЗадания` | ScheduledJob | `ScheduledJobs` |
| `ФункциональныеОпции` | FunctionalOption | `FunctionalOptions` |
| `ПараметрыФункциональныхОпций` | FunctionalOptionsParameter | `FunctionalOptionsParameters` |
| `ОпределяемыеТипы` | DefinedType | `DefinedTypes` |
| `ХранилищаНастроек` | SettingsStorage | `SettingsStorages` |
| `ОбщиеФормы` | CommonForm | `CommonForms` |
| `ОбщиеКоманды` | CommonCommand | `CommonCommands` |
| `ГруппыКоманд` | CommandGroup | `CommandGroups` |
| `ОбщиеМакеты` | CommonTemplate | `CommonTemplates` |
| `ОбщиеКартинки` | CommonPicture | `CommonPictures` |
| `XDTOПакеты` | XDTOPackage | `XDTOPackages` |
| `WebСервисы` | WebService | `WebServices` |
| `WSСсылки` | WSReference | `WSReferences` |
| `HTTPСервисы` | HTTPService | `HTTPServices` |
| `СервисыИнтеграции` | IntegrationService | `IntegrationServices` |
| `ЭлементыСтиля` | StyleItem | `StyleItems` |
| `Стили` | Style | `Styles` |
| `Языки` | Language | `Languages` |
| `Константы` | Constant | `Constants` |
| `Справочники` | Catalog | `Catalogs` |
| `Документы` | Document | `Documents` |
| `ЖурналыДокументов` | DocumentJournal | `DocumentJournals` |
| `Нумераторы` | DocumentNumerator | `DocumentNumerators` |
| `Последовательности` | Sequence | `Sequences` |
| `Перечисления` | Enum | `Enums` |
| `Отчеты` | Report | `Reports` |
| `Обработки` | DataProcessor | `DataProcessors` |
| `ПланыВидовХарактеристик` | ChartOfCharacteristicTypes | `ChartsOfCharacteristicTypes` |
| `ПланыСчетов` | ChartOfAccounts | `ChartsOfAccounts` |
| `ПланыВидовРасчета` | ChartOfCalculationTypes | `ChartsOfCalculationTypes` |
| `РегистрыСведений` | InformationRegister | `InformationRegisters` |
| `РегистрыНакопления` | AccumulationRegister | `AccumulationRegisters` |
| `РегистрыБухгалтерии` | AccountingRegister | `AccountingRegisters` |
| `РегистрыРасчета` | CalculationRegister | `CalculationRegisters` |
| `БизнесПроцессы` | BusinessProcess | `BusinessProcesses` |
| `Задачи` | Task | `Tasks` |
| `ВнешниеИсточникиДанных` | ExternalDataSource | `ExternalDataSources` |

### 2.2. Псевдо-корни — один экземпляр

| Путь | Что это |
|---|---|
| `Конфигурация` | Узел корня основной конфигурации, для свойств уровня конфигурации. |
| `Расширение` | Узел корня расширения. |

Псевдо-корни используются как путь для `v8vscedit_configuration_info`,
`v8vscedit_validate_configuration`, `v8vscedit_get_properties`,
`v8vscedit_set_property_by_path` и `parentPath` в `v8vscedit_compile_subsystem`
и `v8vscedit_compile_role`.

### 2.3. Прямые дочерние сегменты — без префикса

| Контекст | Что разрешено напрямую |
|---|---|
| Справочник, Документ, ПланВидовХарактеристик, ПланВидовРасчета, ПланСчетов, БизнесПроцесс, Задача, Отчёт, Обработка | `Реквизит` |
| те же | `ТабличнаяЧасть` |
| РегистрСведений, РегистрНакопления, РегистрБухгалтерии, РегистрРасчёта | `Измерение`, `Ресурс`, `Реквизит` |
| Перечисление | `ЗначениеПеречисления` |
| ЖурналДокументов | `Графа` |
| Подсистема | вложенная `Подсистема` |

Принцип: имена в этих категориях уникальны внутри объекта (1С это гарантирует
валидатором имён), поэтому сегмент-роль не нужен. Резолвер падает с понятным
сообщением, если по случайности пользовательская выгрузка содержит одноимённые
реквизит и табличную часть.

### 2.4. Сегментированные дочерние сегменты — с префиксом

| Сегмент пути | Где применяется |
|---|---|
| `СтандартныйРеквизит` | псевдо-реквизиты `Код`, `Наименование`, `Ссылка`, `Дата`, `Номер`, `ПометкаУдаления`, … |
| `Форма` | везде, где есть формы |
| `Команда` | везде, где есть команды |
| `Макет` | везде, где есть макеты |
| `Реквизит` | **внутри** `ТабличнаяЧасть.Имя` (колонки ТЧ) |
| `РеквизитАдресации` | у Задачи |
| `ПризнакУчета` | у ПланСчетов |
| `ПризнакУчетаСубконто` | у ПланСчетов |
| `Перерасчет` | у РегистрРасчёта |
| `Таблица`, `Куб`, `Функция` | у ВнешнегоИсточникаДанных |
| `URLШаблон` | у HTTPСервиса (третий уровень: HTTPСервис → URL-шаблон → Метод) |
| `Метод` | **внутри** `URLШаблон.Имя` — тот же контейнерный паттерн, что и `Реквизит` внутри `ТабличнаяЧасть.Имя` |

### 2.5. Модули — сегмент в хвосте

| Путь | Файл BSL |
|---|---|
| `Справочники.X.МодульОбъекта` | `Catalogs/X/Ext/ObjectModule.bsl` |
| `Справочники.X.МодульМенеджера` | `Catalogs/X/Ext/ManagerModule.bsl` |
| `Документы.X.МодульОбъекта` / `МодульМенеджера` | аналогично |
| `РегистрыСведений.X.МодульНабораЗаписей` | `InformationRegisters/X/Ext/RecordSetModule.bsl` |
| `РегистрыСведений.X.МодульМенеджера` | `InformationRegisters/X/Ext/ManagerModule.bsl` |
| `Константы.X.МодульМенеджераЗначения` | `Constants/X/Ext/ValueManagerModule.bsl` |
| `Справочники.X.Форма.Y.МодульФормы` | `Catalogs/X/Forms/Y/Ext/Form/Module.bsl` |
| `Справочники.X.Команда.Z.МодульКоманды` | `Catalogs/X/Commands/Z/Ext/CommandModule.bsl` |
| `ОбщиеМодули.X` | `CommonModules/X/Ext/Module.bsl` (модуль = сам объект) |
| `ОбщиеФормы.X.МодульФормы` | `CommonForms/X/Ext/Form/Module.bsl` |
| `ОбщиеКоманды.X.МодульКоманды` | `CommonCommands/X/Ext/CommandModule.bsl` |
| `WebСервисы.X.МодульМенеджера` | `WebServices/X/Ext/Module.bsl` |
| `HTTPСервисы.X.МодульМенеджера` | `HTTPServices/X/Ext/Module.bsl` |
| `Конфигурация.МодульУправляемогоПриложения` | `Ext/ManagedApplicationModule.bsl` |
| `Конфигурация.МодульСеанса` | `Ext/SessionModule.bsl` |
| `Конфигурация.МодульВнешнегоСоединения` | `Ext/ExternalConnectionModule.bsl` |
| `Конфигурация.МодульОбычногоПриложения` | `Ext/OrdinaryApplicationModule.bsl` |

Для `ОбщиеМодули` сегмент-хвост пустой: общий модуль и есть модуль объекта,
отдельного `.МодульОбъекта` не пишут.

### 2.6. Расширенные примеры путей

```text
Справочники.Контрагенты
Справочники.Контрагенты.ИНН                                  ← реквизит
Справочники.Контрагенты.КПП                                  ← реквизит
Справочники.Контрагенты.СтандартныйРеквизит.Код              ← стандартный реквизит
Справочники.Контрагенты.СтандартныйРеквизит.Наименование
Справочники.Контрагенты.СтандартныйРеквизит.ПометкаУдаления
Справочники.Контрагенты.КонтактныеЛица                       ← табличная часть
Справочники.Контрагенты.ТабличнаяЧасть.КонтактныеЛица.Реквизит.ФИО
Справочники.Контрагенты.ТабличнаяЧасть.КонтактныеЛица.Реквизит.Телефон
Справочники.Контрагенты.Форма.ФормаСписка
Справочники.Контрагенты.Форма.ФормаЭлемента
Справочники.Контрагенты.Форма.ФормаЭлемента.МодульФормы
Справочники.Контрагенты.Команда.Печать
Справочники.Контрагенты.Команда.Печать.МодульКоманды
Справочники.Контрагенты.Макет.СчетФактура
Справочники.Контрагенты.МодульОбъекта
Справочники.Контрагенты.МодульМенеджера

Документы.РасходТовара
Документы.РасходТовара.Контрагент                            ← реквизит-шапка
Документы.РасходТовара.Товары                                ← табличная часть
Документы.РасходТовара.ТабличнаяЧасть.Товары.Реквизит.Номенклатура
Документы.РасходТовара.ТабличнаяЧасть.Товары.Реквизит.Количество
Документы.РасходТовара.Форма.ФормаДокумента.МодульФормы

РегистрыСведений.КурсыВалют
РегистрыСведений.КурсыВалют.Валюта                           ← измерение
РегистрыСведений.КурсыВалют.Курс                             ← ресурс
РегистрыСведений.КурсыВалют.Комментарий                      ← реквизит
РегистрыСведений.КурсыВалют.МодульНабораЗаписей
РегистрыСведений.КурсыВалют.МодульМенеджера

РегистрыНакопления.ТоварыНаСкладах.Склад                     ← измерение
РегистрыНакопления.ТоварыНаСкладах.Количество                ← ресурс

РегистрыРасчета.Зарплата.Перерасчет.Сторнирование

Перечисления.СтатусыЗаказа
Перечисления.СтатусыЗаказа.Активен                           ← значение перечисления
Перечисления.СтатусыЗаказа.Завершён

Константы.КурсДоллара
Константы.КурсДоллара.МодульМенеджераЗначения

ЖурналыДокументов.Складские.Контрагент                       ← графа

Задачи.ВыполнитьПоручение
Задачи.ВыполнитьПоручение.РеквизитАдресации.Исполнитель
Задачи.ВыполнитьПоручение.РеквизитАдресации.Подразделение

ПланыСчетов.Основной.ПризнакУчета.Количественный

ОбщиеМодули.ОбщегоНазначения                                 ← сам общий модуль
ОбщиеМодули.ОбщегоНазначенияСервер
ОбщиеКоманды.ГлобальныйПоиск.МодульКоманды
ОбщиеФормы.ФормаНастроек.МодульФормы

Подсистема.Продажи
Подсистема.Продажи.Розница
Подсистема.Продажи.Розница.Возвраты

Роли.БазовыеПрава
Роли.Администратор

HTTPСервисы.МойСервис
HTTPСервисы.МойСервис.МодульМенеджера
HTTPСервисы.МойСервис.URLШаблон.Товары                       ← URL-шаблон
HTTPСервисы.МойСервис.URLШаблон.Товары.Метод.ПолучитьСписок  ← метод внутри шаблона

Отчеты.ПродажиЗаПериод
Отчеты.ПродажиЗаПериод.Макет.ОсновнаяСхема
Отчеты.ПродажиЗаПериод.Форма.ФормаНастроек

Конфигурация
Конфигурация.МодульУправляемогоПриложения
Конфигурация.МодульСеанса

Расширение
```

---

## 3. Канон типов значений 1С

Раздел соответствует главе 2 плана. Реестр типов выдаётся инструментом
`v8vscedit_list_supported_types` (платформенные типы) и
`v8vscedit_list_available_types` (с учётом конфигурации и контекста свойства).
Имена, передаваемые в `v8vscedit_set_type`, — только русские из этого
реестра.

### 3.1. Базовые платформенные типы (новинка: ключевая часть E2)

Базовый реестр платформенных типов теперь живёт в `infra/xml/PlatformTypeRegistry.ts`
и читает данные из `domain/CanonicalNames.ts`. Это ключевая новинка
рефакторинга MCP — раньше эти типы были захардкожены в нескольких местах.
В частности, добавлена поддержка `УникальныйИдентификатор` (XML-токен `v8:UUID`)
как полноценного платформенного типа реквизитов прикладных объектов
и форм.

| Канон (рус) | XML | Контекст |
|---|---|---|
| Строка | String | оба |
| Число | Number | оба |
| Булево | Boolean | оба |
| Дата | Date | оба |
| ДатаВремя | DateTime | оба |
| ХранилищеЗначения | ValueStorage | оба |
| УникальныйИдентификатор | UUID | оба |
| ДвоичныеДанные | BinaryData | оба |
| Картинка | Picture | оба |
| Тип | Type | реквизит формы |
| ОписаниеТипов | TypeDescription | реквизит формы |
| СписокЗначений | ValueList | только формы |
| ТаблицаЗначений | ValueTable | только формы |
| ДеревоЗначений | ValueTree | только формы |
| Массив | Array | только формы |
| ФиксированныйМассив | FixedArray | только формы |
| Структура | Structure | только формы |
| ФиксированнаяСтруктура | FixedStructure | только формы |
| ТабличныйДокумент | SpreadsheetDocument | только формы |
| ФорматированнаяСтрока | FormattedString | только формы |

Контекст:
- «оба» — допустим как тип реквизита прикладного объекта и как тип реквизита формы.
- «только формы» — нельзя как тип реквизита метаданных, только в форме.

### 3.2. Производные ссылочные типы — генерируются по конфигурации

Производные типы генерируются по фактическому составу выбранной конфигурации.
Чтобы получить их, инструмент должен видеть конфигурацию (передан `configuration`,
либо в workspace единственная конфигурация).

| Корневой тип метаданных | Производные (русский ↔ XML) |
|---|---|
| Справочник | СправочникСсылка ↔ CatalogRef, СправочникОбъект ↔ CatalogObject, СправочникМенеджер ↔ CatalogManager, СправочникВыборка ↔ CatalogSelection, СправочникСписок ↔ CatalogList |
| Документ | ДокументСсылка ↔ DocumentRef, ДокументОбъект ↔ DocumentObject, ДокументМенеджер ↔ DocumentManager, ДокументВыборка ↔ DocumentSelection, ДокументСписок ↔ DocumentList |
| Перечисление | ПеречислениеСсылка ↔ EnumRef, ПеречислениеМенеджер ↔ EnumManager, ПеречислениеСписок ↔ EnumList |
| ПланВидовХарактеристик | ПланВидовХарактеристикСсылка/Объект/Менеджер ↔ ChartOfCharacteristicTypesRef/Object/Manager |
| ПланСчетов | ПланСчетовСсылка/Объект/Менеджер ↔ ChartOfAccountsRef/Object/Manager |
| ПланВидовРасчета | ПланВидовРасчетаСсылка/Объект/Менеджер ↔ ChartOfCalculationTypesRef/Object/Manager |
| ПланОбмена | ПланОбменаСсылка/Объект/Менеджер ↔ ExchangePlanRef/Object/Manager |
| БизнесПроцесс | БизнесПроцессСсылка/Объект/Менеджер ↔ BusinessProcessRef/Object/Manager, БизнесПроцессТочкаМаршрутаСсылка ↔ BusinessProcessRoutePointRef |
| Задача | ЗадачаСсылка/Объект/Менеджер ↔ TaskRef/Object/Manager |
| РегистрСведений | РегистрСведенийНаборЗаписей ↔ InformationRegisterRecordSet, РегистрСведенийКлючЗаписи ↔ …RecordKey, РегистрСведенийМенеджерЗаписи ↔ …RecordManager, РегистрСведенийВыборка ↔ …Selection, РегистрСведенийСписок ↔ …List, РегистрСведенийМенеджер ↔ …Manager |
| РегистрНакопления | те же 6 (Accumulation…) |
| РегистрБухгалтерии | те же 6 (Accounting…) |
| РегистрРасчета | те же 6 (Calculation…) + РегистрРасчетаПерерасчет ↔ CalculationRegisterRecalculation |
| Константа | КонстантаМенеджер ↔ ConstantManager, КонстантаМенеджерЗначения ↔ ConstantValueManager |
| Обработка | ОбработкаОбъект ↔ DataProcessorObject, ОбработкаМенеджер ↔ DataProcessorManager |
| Отчет | ОтчетОбъект ↔ ReportObject, ОтчетМенеджер ↔ ReportManager |
| ОпределяемыйТип | ОпределяемыйТип ↔ DefinedType |
| ХранилищеНастроек | ХранилищеНастроекМенеджер ↔ SettingsStorageManager |

Фильтрация по контексту свойства:

- **Тип реквизита прикладного объекта** (`metadataAttribute`) — примитивы из §3.1
  с пометкой «оба» + только `*Ссылка` и `ОпределяемыйТип` из §3.2.
- **Тип реквизита формы** (`formAttribute`) — всё из §3.1 + всё из §3.2 + `DynamicList`.
- **Источник подписки на событие** (`eventSource`) — только `*Объект`, `*НаборЗаписей`,
  `КонстантаМенеджерЗначения`.
- **Тип параметра команды** (`commandParameter`) — только `*Ссылка` и `ОпределяемыйТип`.

### 3.3. Квалификаторы типов

Квалификаторы передаются в `v8vscedit_set_type` как отдельные поля
(`length`, `digits`, `fractionDigits`, `dateFractions`, `allowedSign`,
`allowedLength`).

| Канон (рус) | XML | Где |
|---|---|---|
| Длина | length | Строка |
| ДопустимаяДлина | allowedLength: `Переменная` / `Фиксированная` | Строка |
| Разрядность | digits | Число |
| РазрядностьДробнойЧасти | fractionDigits | Число |
| ДопустимыйЗнак | allowedSign: `Любой` / `Неотрицательный` | Число |
| СоставДаты | dateFractions: `Дата` / `Время` / `ДатаВремя` | Дата |

---

## 4. Запреты — что MCP отвергает

MCP-инструменты принимают только канон. Любая попытка передать одну из форм
ниже завершается ошибкой резолвера с сообщением «используй канон».

### 4.1. Английские формы

- `Catalog.X`, `Catalogs.X` — английские имена корней не принимаются ни в одном
  числе. Используй `Справочники.X`.
- `Document.X`, `Documents.X` — то же. Используй `Документы.X`.
- `InformationRegister.X`, `InformationRegisters.X` — используй `РегистрыСведений.X`.
- Внутренние сегменты тоже только русские: `Form.Y` → `Форма.Y`,
  `Command.Z` → `Команда.Z`, `Template.X` → `Макет.X`, `ObjectModule` → `МодульОбъекта`.

### 4.2. Единственное число для коллекций

- `Справочник.Контрагенты` — это форма запроса 1С, а не путь метаданных.
  В MCP — только `Справочники.Контрагенты`.
- `Документ.РасходТовара` → `Документы.РасходТовара`.
- `РегистрСведений.КурсыВалют` → `РегистрыСведений.КурсыВалют`.

Исключение — те самые корни, у которых единственное число каноническое:
`Подсистема`, `Конфигурация`, `Расширение`.

### 4.3. Грязные сегменты

- `Справочники.X.Реквизиты.Y` — лишний сегмент-роль `Реквизиты` (мн.ч.).
  Реквизит присоединяется прямо: `Справочники.X.Y`.
- `Справочники.X.ТабличныеЧасти.T` — лишний сегмент `ТабличныеЧасти` (мн.ч.).
  Используй прямую форму: `Справочники.X.T`.
- `Справочники.X.Формы.Y` — мн.ч. вместо канонического `Форма` (ед.ч.).
  Используй `Справочники.X.Форма.Y`.
- `Справочники.X.Команды.Z` → `Справочники.X.Команда.Z`.

Соответствующие сегментированные роли пишутся в единственном числе: `Форма`,
`Команда`, `Макет`, `СтандартныйРеквизит`, `Реквизит` (только внутри ТЧ),
`РеквизитАдресации`, `ПризнакУчета`, `ПризнакУчетаСубконто`, `Перерасчет`.

### 4.4. Удвоение префикса у подсистем

- `Подсистема.А.Подсистема.Б.Подсистема.В` — повтор сегмента `Подсистема`
  для каждого уровня иерархии. Канон — единичный префикс:
  `Подсистема.А.Б.В`.

### 4.5. Точка внутри имени

Имя объекта 1С не содержит точку — это запрещено платформой. MCP-резолвер
любую точку трактует как разделитель сегментов. Это уберегает агентов от
двусмысленных путей, которые пришлось бы парсить «как платформа».

---

## 5. Как использовать с MCP-инструментами

### 5.1. Общий аргумент `path`

После рефакторинга E5 у всех инструментов, работающих с одним узлом, аргумент
называется одинаково: `path`. Никаких `objectPath`, `formPath`, `templatePath`,
`rightsPath`, `subsystemPath`, `ciPath` и `modulePath` больше нет. Если
инструмент работает с несколькими путями сразу (`v8vscedit_validate_metadata`),
он принимает массив через `paths`.

Парные инструменты, создающие новый объект внутри родителя
(`v8vscedit_compile_subsystem`, `v8vscedit_compile_role`,
`v8vscedit_add_form`), принимают `parentPath` — канонический путь
родителя (`Конфигурация`, `Расширение`, `Подсистема.X.Y` или корневой
объект).

### 5.2. `v8vscedit_tree` для обзора

Перед любой массовой работой с конфигурацией удобно один раз получить плоский
список канонических путей через `v8vscedit_tree`:

```json
{
  "configuration": "cf",
  "scope": "objects",
  "depth": "objects",
  "include": ["Справочники", "Документы"],
  "limit": 5000
}
```

`tree` возвращает плоский массив строк, без свойств — за свойствами идти
прицельно через `metadata_info`. Это снимает необходимость в пагинации `list`
на больших конфигурациях.

Допустимые значения:
- `scope`: `all` | `objects` | `common` | `subsystems`.
- `depth`: `roots` (только корни), `objects` (корни + объекты), `children`
  (корни + объекты + дочерние элементы первого уровня).

### 5.3. `v8vscedit_resolve_path` для отладки

Если непонятно, как выглядит канон для конкретного объекта в работе агента, —
передай сомнительный вход в `v8vscedit_resolve_path` и посмотри `canonical`.
Инструмент нормализует путь и проверяет существование узла:

```json
{ "path": "Справочники.Контрагенты", "configuration": "cf" }
```

Результат:

```json
{
  "canonical": "Справочники.Контрагенты",
  "exists": true,
  "nodeKind": "Catalog",
  "xmlPath": "…/Catalogs/Контрагенты.xml"
}
```

Если узла нет — `exists: false` без ошибки. Если форма не канон — ошибка с
подсказкой канона.

### 5.4. `v8vscedit_list_supported_types` для выбора типа

Полезен перед `v8vscedit_set_type`, особенно когда конфигурация большая и
агент не знает, какие справочники там есть.

```json
{ "context": "metadataAttribute", "configuration": "cf" }
```

Возвращает группы примитивов и ссылочных типов, отфильтрованные по контексту.
Базовая группа выдаётся даже без `configuration`; ссылочные группы появляются
только при выбранной конфигурации.

---

## 6. Полный список MCP-инструментов

Таблица сгенерирована из актуального состава MCP-инструментов расширения
после E5. Регистрация физически разнесена по доменам в `src/ui/mcp/registration/*`
(`V8McpServer.ts` — тонкий фасад: HTTP-транспорт, сессии, Host/Origin-защита; см.
[architecture.md](./architecture.md#декомпозиция-god-классов-на-тонкий-фасад--подмодули)),
но публичный состав и контракт инструментов от этого не меняются. Для
большинства инструментов общий аргумент — `path` в каноне. Инструменты без
`path` указаны явно.

| Инструмент | Что делает | Формат `path` |
|---|---|---|
| `v8vscedit_workspace_overview` | Основная конфигурация, расширения, корни, счётчики объектов. | — |
| `v8vscedit_search_metadata` | Поиск по части строки в путях метаданных. | — (поиск, не путь) |
| `v8vscedit_list_metadata` | Список объектов группы или дочерних по пути. | `parentPath` в каноне |
| `v8vscedit_tree` | Плоский массив канонических путей конфигурации. | — (см. §5.2) |
| `v8vscedit_resolve_path` | Нормализует путь и проверяет существование узла. | любой канон |
| `v8vscedit_list_supported_types` | Реестр платформенных типов по контексту. | — |
| `v8vscedit_configuration_info` | Свойства и счётчики ChildObjects. | `Конфигурация` / `Расширение` |
| `v8vscedit_validate_configuration` | Валидирует Configuration.xml и ChildObjects. | `Конфигурация` / `Расширение` |
| `v8vscedit_metadata_info` | Структура объекта: реквизиты, ТЧ, формы, команды, макеты. | `Справочники.X`, `Документы.X`, … |
| `v8vscedit_validate_metadata` | Валидирует XML объекта (поддерживает массив `paths`). | `Справочники.X` или массив |
| `v8vscedit_subsystem_info` | Свойства подсистемы, состав, дерево, CommandInterface. | `Подсистема.X.Y` |
| `v8vscedit_validate_subsystem` | Валидирует XML подсистемы и CommandInterface.xml. | `Подсистема.X.Y` |
| `v8vscedit_mxl_info` | Сводка MXL: области, параметры, текст, статистика. | `Справочники.X.Макет.Y` / `ОбщиеМакеты.X` |
| `v8vscedit_validate_mxl` | Валидирует Template.xml табличного документа. | как `mxl_info` |
| `v8vscedit_compile_mxl` | Перезаписывает MXL из JSON DSL. | `Справочники.X.Макет.Y` |
| `v8vscedit_decompile_mxl` | Возвращает JSON DSL по существующему MXL. | как `mxl_info` |
| `v8vscedit_skd_info` | Сводка СКД: наборы, поля, итоги, параметры, варианты. | `Отчеты.X.Макет.ОсновнаяСхема` |
| `v8vscedit_validate_skd` | Валидирует СКД Template.xml. | как `skd_info` |
| `v8vscedit_compile_skd` | Перезаписывает СКД из JSON DSL. | как `skd_info` |
| `v8vscedit_edit_skd` | Точечное редактирование СКД (поля, итоги, фильтры, …). | как `skd_info` |
| `v8vscedit_add_help` | Создаёт Ext/Help.xml и Help/<lang>.html для объекта. | `Справочники.X`, `Документы.X` |
| `v8vscedit_create_epf` | Создаёт XML-исходники внешней обработки EPF. | — (использует `outputDir`) |
| `v8vscedit_create_erf` | Создаёт XML-исходники внешнего отчёта ERF. | — (использует `outputDir`) |
| `v8vscedit_validate_external_object` | Валидирует EPF/ERF XML. | `objectPath` (FS) |
| `v8vscedit_epf_bsp_init` | Добавляет регистрацию БСП в ObjectModule.bsl. | `objectPath` (FS) |
| `v8vscedit_epf_bsp_add_command` | Добавляет команду в БСП-регистрацию. | `objectPath` (FS) |
| `v8vscedit_form_info` | Структура управляемой формы: элементы, реквизиты, команды. | `Справочники.X.Форма.Y` / `ОбщиеФормы.X` |
| `v8vscedit_validate_form` | Валидирует Form.xml: ID, DataPath, callType, типы. | как `form_info` |
| `v8vscedit_add_form` | Создаёт форму объекта с регистрацией в ChildObjects. | `parentPath = Справочники.X`, `name` |
| `v8vscedit_remove_form` | Удаляет форму и снимает регистрацию. | `Справочники.X.Форма.Y` |
| `v8vscedit_compile_form` | Перезаписывает Form.xml из JSON DSL. | как `form_info` |
| `v8vscedit_edit_form` | Точечное редактирование формы. | как `form_info` |
| `v8vscedit_compile_subsystem` | Создаёт подсистему и регистрирует в родителе. | `parentPath = Конфигурация / Расширение / Подсистема.X` |
| `v8vscedit_edit_subsystem_content` | Меняет Content подсистемы. | `Подсистема.X.Y` |
| `v8vscedit_edit_command_interface` | Hide/show/place/order в CommandInterface.xml подсистемы. | `Подсистема.X.Y` |
| `v8vscedit_validate_command_interface` | Валидирует CommandInterface.xml. | `Подсистема.X.Y` |
| `v8vscedit_role_info` | Права, RLS и шаблоны из Rights.xml. | `Роли.X` |
| `v8vscedit_validate_role` | Валидирует Rights.xml и регистрацию роли. | `Роли.X` |
| `v8vscedit_compile_role` | Создаёт роль и регистрирует в Configuration.xml. | `parentPath = Конфигурация / Расширение` |
| `v8vscedit_edit_role` | Точечное редактирование прав роли. | `Роли.X` |
| `v8vscedit_list_default_roles` | Список `<DefaultRoles>` из Configuration.xml. | `Конфигурация` / `Расширение` |
| `v8vscedit_add_default_role` | Добавляет роль в `<DefaultRoles>`. | `Конфигурация` / `Расширение` |
| `v8vscedit_remove_default_role` | Удаляет роль из `<DefaultRoles>`. | `Конфигурация` / `Расширение` |
| `v8vscedit_set_default_roles` | Полностью заменяет `<DefaultRoles>`. | `Конфигурация` / `Расширение` |
| `v8vscedit_create_configuration` | Создаёт scaffold CF (Configuration.xml + язык). | — (использует `outputDir`) |
| `v8vscedit_create_extension` | Создаёт scaffold CFE. | — (использует `outputDir`) |
| `v8vscedit_get_properties` | ВСЕ свойства узла по пути: значения, readonly, enum/multiEnum-значения; для `metadataType` в `notes` — какой tool вызывать. | любой канон узла |
| `v8vscedit_set_property_by_path` | Меняет одно простое свойство по пути. | любой канон узла |
| `v8vscedit_set_properties` | Пакетная смена нескольких простых свойств одного узла за один вызов; типы — через `v8vscedit_set_type`. | любой канон узла |
| `v8vscedit_list_available_types` | Стандартные и конфигурационные типы для свойства. | канон узла либо пусто |
| `v8vscedit_set_type` | Меняет свойство Тип / Источник / параметра команды. | канон узла |
| `v8vscedit_rename_metadata` | Переименовывает объект или дочерний элемент. | канон узла |
| `v8vscedit_remove_metadata` | Удаляет объект или дочерний элемент. | канон узла |
| `v8vscedit_add_*` (корни) | См. §6.1 — отдельный tool на каждый корневой тип метаданных. | — |
| `v8vscedit_add_*` (дети) | См. §6.2 — отдельный tool на каждый дочерний тип. | `path` к объекту-владельцу |
| `v8vscedit_cfe_borrow` | Заимствует объект, форму или дочерний элемент из CF в CFE. | — (FS-аргументы) |
| `v8vscedit_cfe_patch_method` | BSL-перехватчик &Перед/&После/&ИзменениеИКонтроль в CFE. | `Справочники.X.МодульОбъекта`, … |
| `v8vscedit_cfe_diff` | Состав расширения и проверка переноса #Вставка. | — (`extensionPath` FS) |
| `v8vscedit_execute_command` | Allowlist команд расширения: `refresh`, `importConfigurations`, `updateChangedConfigurations`. Ответ — явный исход `outcome` (`done`/`no-changes`/`no-targets`/`cancelled`/`busy`/`failed`); при `busy` — `heldBy` с заголовком держащей операции. | — |

Несколько инструментов (`create_epf`/`create_erf`/`validate_external_object`/
`epf_bsp_*`/`cfe_borrow`/`cfe_diff`) работают с EPF/ERF/CFE по обычному
файловому пути, потому что объект ещё не зарегистрирован в дереве
конфигурации. Для них канон путей метаданных неприменим, аргументы остаются
файловыми (`objectPath`, `extensionPath`, `outputDir`).

### 6.1. Tools добавления корневых объектов

Бывший общий `v8vscedit_add_metadata_by_path` декомпозирован на отдельные
инструменты — один на каждый корневой тип метаданных. Все принимают
`name` (имя нового объекта) и опциональный `configuration`; `v8vscedit_add_common_template`
дополнительно принимает `templateType` (`Табличный документ`, `Текстовый документ`,
`HTML`, `Двоичные данные`, `СКД`, `Графическая схема`, `Внешняя компонента`).

| Инструмент | Создаёт |
|---|---|
| `v8vscedit_add_subsystem` | Подсистему первого уровня (вложенные — через `compile_subsystem`). |
| `v8vscedit_add_common_module` | Общий модуль. |
| `v8vscedit_add_session_parameter` | Параметр сеанса. |
| `v8vscedit_add_role` | Роль с пустым Rights.xml. |
| `v8vscedit_add_common_attribute` | Общий реквизит. |
| `v8vscedit_add_exchange_plan` | План обмена. |
| `v8vscedit_add_filter_criterion` | Критерий отбора. |
| `v8vscedit_add_event_subscription` | Подписку на событие. |
| `v8vscedit_add_scheduled_job` | Регламентное задание. |
| `v8vscedit_add_functional_option` | Функциональную опцию. |
| `v8vscedit_add_functional_options_parameter` | Параметр функциональных опций. |
| `v8vscedit_add_defined_type` | Определяемый тип. |
| `v8vscedit_add_settings_storage` | Хранилище настроек. |
| `v8vscedit_add_common_form` | Общую форму (Form.xml + Module.bsl). |
| `v8vscedit_add_common_command` | Общую команду. |
| `v8vscedit_add_command_group` | Группу команд. |
| `v8vscedit_add_common_template` | Общий макет (с `templateType`). |
| `v8vscedit_add_common_picture` | Общую картинку. |
| `v8vscedit_add_xdto_package` | XDTO-пакет. |
| `v8vscedit_add_web_service` | Web-сервис (SOAP). |
| `v8vscedit_add_http_service` | HTTP-сервис. |
| `v8vscedit_add_ws_reference` | WS-ссылку. |
| `v8vscedit_add_integration_service` | Сервис интеграции. |
| `v8vscedit_add_style_item` | Элемент стиля. |
| `v8vscedit_add_style` | Стиль. |
| `v8vscedit_add_language` | Язык. |
| `v8vscedit_add_constant` | Константу. |
| `v8vscedit_add_catalog` | Справочник с базовыми обязательными свойствами. |
| `v8vscedit_add_document` | Документ. |
| `v8vscedit_add_document_journal` | Журнал документов. |
| `v8vscedit_add_document_numerator` | Нумератор документов. |
| `v8vscedit_add_sequence` | Последовательность документов. |
| `v8vscedit_add_enum` | Перечисление. |
| `v8vscedit_add_report` | Отчёт. |
| `v8vscedit_add_data_processor` | Обработку. |
| `v8vscedit_add_chart_of_characteristic_types` | План видов характеристик. |
| `v8vscedit_add_chart_of_accounts` | План счетов. |
| `v8vscedit_add_chart_of_calculation_types` | План видов расчёта. |
| `v8vscedit_add_information_register` | Регистр сведений. |
| `v8vscedit_add_accumulation_register` | Регистр накопления. |
| `v8vscedit_add_accounting_register` | Регистр бухгалтерии. |
| `v8vscedit_add_calculation_register` | Регистр расчёта. |
| `v8vscedit_add_business_process` | Бизнес-процесс с пустым Flowchart.xml. |
| `v8vscedit_add_task` | Задачу. |
| `v8vscedit_add_external_data_source` | Внешний источник данных. |

### 6.2. Tools добавления дочерних элементов

Каждый инструмент принимает `path` (канонический путь объекта-владельца),
`name` (имя нового элемента) и опциональный `configuration`. Резолвер
проверяет, что тип владельца совместим с типом дочернего элемента: например,
`v8vscedit_add_dimension` потребует регистр, а не справочник.

| Инструмент | Что добавляет | Куда |
|---|---|---|
| `v8vscedit_add_attribute` | Реквизит | Справочник, Документ, ПВХ, ПВР, ПС, БП, Задача, Отчёт, Обработка, ПланОбмена, регистры, общий реквизит. |
| `v8vscedit_add_addressing_attribute` | Реквизит адресации | Только Задача. |
| `v8vscedit_add_tabular_section` | Табличную часть | Справочник, Документ, ПВХ, ПВР, ПС, БП, Задача, Отчёт, Обработка, ПланОбмена. |
| `v8vscedit_add_command` | Команду | Объекты с командами (справочники, документы, регистры, перечисления, журналы документов). |
| `v8vscedit_add_template` | Макет | Объекты с макетами (`templateType` — тип содержимого). |
| `v8vscedit_add_dimension` | Измерение | Только регистры сведений / накопления / бухгалтерии / расчёта. |
| `v8vscedit_add_resource` | Ресурс | Только регистры. |
| `v8vscedit_add_enum_value` | Значение перечисления | Только Перечисление. |
| `v8vscedit_add_column` | Колонку (реквизит ТЧ) | `path` указывает на табличную часть (`Справочники.X.КонтактныеЛица`). |
| `v8vscedit_add_url_template` | URL-шаблон | Только HTTPСервис; `path` = `HTTPСервисы.X`. |
| `v8vscedit_add_method` | Метод | `path` указывает на URL-шаблон (`HTTPСервисы.X.URLШаблон.T`) — тот же приём, что у `v8vscedit_add_column` с табличной частью. |

Для добавления формы используется отдельный `v8vscedit_add_form` с
аргументами `parentPath`/`name`/`purpose`/`autoFill` — он создаёт не только
запись в ChildObjects, но и Form.xml, Module.bsl и регистрирует форму
в `DefaultForm`/`DefaultObjectForm` при необходимости.

---

## 7. Дополнительные замечания

- Если в workspace несколько конфигураций (`cf` + одно или несколько `cfe/*`),
  большинство инструментов требует параметр `configuration`. Сообщение об
  ошибке перечисляет доступные имена.
- Для CFE канон путей тот же — `Справочники.X`, потому что заимствованный
  объект имеет тот же корневой сегмент. Признак «заимствованный» возвращается
  в DTO выдачи (`borrowed: true`).
- Пути в выдаче всегда канонические. Никаких параллельных алиасов в ответе.
- Если в имени объекта по выгрузке оказался пробел (платформа теоретически
  допускает, EDT — обычно нет), резолвер примет такой узел, но в выдаче
  выделит его кавычками: `Справочники."Имя с пробелом"`. Это маркер опасности —
  такой объект лучше переименовать.
- **Отложено (не реализовано):** read-only-инструмент `v8vscedit_list_database_extensions`
  — список расширений, подключённых к базе проекта (тот же источник, что
  QuickPick команды `v8vscedit.connectExtension`), для ИИ-агентов, планирующих
  подключение/выгрузку расширения без ручного опроса пользователя. Переиспользует
  готовую связку `ExtensionCommandRunner.listConnectedDatabaseExtensions` → CLI
  `list-db-extensions` → `infra/environment/ExtensionListParser.ts` (см.
  [architecture.md](./architecture.md#паттерн-чтение-данных-из-базы-через-пакетный-конфигуратор-file-handoff)) —
  бизнес-логику писать заново не нужно, только регистрация tool'а в
  `V8McpServer`/доменном модуле `registration/`.
