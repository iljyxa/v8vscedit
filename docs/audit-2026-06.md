# Карта проблем (аудит кода, 2026-06-22)

Глубокий аудит всего `src/` (~57k строк, 309 файлов) + `src-ui/` (68 файлов) шестью параллельными ревьюерами. На момент аудита `npm run lint` и `npm run typecheck` — чисто; всё ниже — логические/runtime/security-проблемы, не ловящиеся компилятором.

Статусы: ✅ исправлено и проверено (361 тест проходит) · ⬜ открыто.

---

## P0 — Критические

### Порча данных при записи/редактировании XML
- ✅ **BOM/EOL теряются при записи** — `SubsystemXmlService` (updateProperty/updateContentRefs/updateChildSubsystems), `MetadataXmlCreator.addChildElement`, `MetadataXmlRemover` (removeChildElement/removeFromSubsystems) писали `fs.writeFileSync(...,'utf-8')`. Переведено на `writeTextFilePreservingBomAndEol`.
- ✅ **`String.replace` с `$`-раскрытием в значении** — порча свободных текстовых полей (Синоним/Комментарий/текст запроса СКД/тело обработчика). Все динамические замены в `ObjectXmlReader`, `ConfigurationXmlEditor`, `SubsystemXmlService`, `CommandInterfaceService`, `DataCompositionSchemaService`, `form/FormEditService`, `form/FormShared` переведены на function-replacer (`() => value`).
- ✅ **DCS rename-parameter без экранирования** — `DataCompositionSchemaService` вставлял новое имя без `escapeXmlText` → невалидный XML. Исправлено + function-replacer.
- ✅ **Не-nesting-aware `<Properties>`** — `TypedFieldPropertyRules.findPropertiesInner` резал блок на первом `</Properties>` (потеря хвоста при записи). Переведено на `findNestingAwareElementRange`.
- ✅ **Детект типа жёстко по префиксу `v8:`** — `TypedFieldPropertyRules.detectFieldTypeCategories` молча удалял валидные свойства при другом префиксе. Сделан namespace-agnostic regex.

### Краши на штатных операциях
- ✅ **CfeBorrowService.buildInternalInfo** — `GENERATED_TYPES[typeName].length` падал TypeError для `CommonModule`/`Subsystem`/`CommonForm`. Исправлено (`?? []`).
- ✅ **getMetaFolder** (`domain/MetaTypes`) — `META_TYPES[kind].folder` падал на незнакомом типе (ломал `cfe_diff`). Исправлено (`?.`).

### Обход защиты
- ✅ **MCP полностью обходит проверки поддержки/хранилища** (`V8McpServer`) — добавлен guard `assertMetadataEditable`/`assertNodeEditable`, БЛОКИРУЮЩИЙ мутацию (как UI) с понятной ошибкой клиенту. Охвачено 15 мутирующих инструментов над существующими .cf-объектами (set_property/set_properties/set_type, rename_metadata, remove_metadata, remove_form, add_form, edit_subsystem_content, edit_command_interface, edit_role, compile_mxl/skd/form, edit_skd/form, add_help). Создание новых объектов/EPF/ERF/расширений и заимствование намеренно не блокируются.

---

## P1 — Высокие (открыто)

| # | Статус | Где | Проблема | Фикс |
|---|--------|-----|----------|------|
| 6 | ✅ | `V8McpServer` транспорт | Нет защиты от DNS-rebinding (только loopback, не Host/Origin) | ручная валидация Host/Origin против набора allowedHosts с реальным портом → HTTP 403 |
| 7 | ✅ | `BslAnalyzerService.download` | Бинарник запускается без проверки SHA256, слепые редиректы, https→http downgrade | сверка `.sha256` (если опубликован), запрет downgrade, лимит 5 редиректов |
| 8 | ✅ | `BslAnalyzerService.httpGetJson` | Нет таймаута → вечное зависание «Проверить обновления» | добавлен таймаут |
| 9 | ✅ | `ProcessRunner`, `DesignerAgentProcess` | Вывод vrunner/1С декодируется как UTF-8 минуя `OutputDecoder`; `lastStdout` хранит только последний чанк | аккумуляция Buffer + `decodeProcessOutput` |
| 10 | ✅ | `LspManager` | Гонка `crashCount`/ручного restart → 2 параллельных процесса bsl-analyzer | введены «поколения» клиента: stale-события `closed` отсекаются, авто-рестарт старого клиента подавляется |
| 11 | ✅ | package.json + `Container` | Мёртвое наследие нативного TreeView: ~30 `view==v8vsceditTree` меню, `this.treeView` не инициализируется | меню удалены, поле `treeView` и мёртвые ветки убраны, `revealTreeNode` → задокументированный no-op |
| 12 | ✅ | `src-ui` request/reply | Мёртвый модуль `requestReply.ts` удалён + кнопка «Обзор» починена: `'reply'` добавлен в `HostToUiMessage`, `RepositoryConnectionApp` слушает `reply` по `requestId` | |
| 13 | ✅ | `BslReadonlyGuard` | readonly ставится только по событию в 5с | readonly применяется синхронно к видимому редактору при открытии; событийный путь — только фолбэк |
| 14 | ✅ | `FormToolsCommands.removeForm` | Удаляет форму без подтверждения и проверок | добавлены проверки поддержки/хранилища + модальное подтверждение |
| 15 | ✅ | `AiMcpViewProvider` | Сырые секреты в webview и в `.vscode/settings.json` (→git) | webview-утечка закрыта (флаги+маска); секреты перенесены в `SecretStorage` с миграцией из настроек и очисткой settings.json; MCP резолвит реальные значения из SecretStorage |
| 16 | ✅ | `AiMcpApp.vue` | `:href` host-строкой без allowlist → `javascript:` XSS | `safeUrl()` пропускает только http/https |
| 17 | ✅ | `ConfigXmlReader`/`XmlUtils` | `processEntities:false`; атрибут-regex не понимает одинарные кавычки | `processEntities:true` (симметрия чтение/запись сохранена); атрибуты читаются и в `'…'`, и в `"…"` |

---

## P2 — Средние

- ✅ **Процессы/ФС:** `DesignerAgentProcess.stop` — SIGKILL-фолбэк + ожидание exit; `ProcessDesignerAgentTransport` больше не реанимирует disposed-сессию; `DirectorySnapshot.replaceDirectorySnapshot` восстанавливает target при сбое; `HashCache` пишет атомарно (tmp+rename).
- ✅ **Утечки disposable:** `statusBarItem` диспозится; `MetadataTreeProvider` получил `dispose()` и зарегистрирован; universal-панель освобождает `onDidReceiveMessage` и слушает `onDidDispose`.
- ✅ **Парсинг:** ложные ссылки `includes('Kind.Name')` → сопоставление с границами идентификатора; `isRegistered` экранирует имя; формы вставляют в КОРНЕВОЙ `<ChildItems>`/`<Events>` (nesting-aware, не AutoCommandBar); атрибуты читаются и в `'…'` (см. #17).
- ✅ **Vue:** `PropertyTypeControl` watch по подполям + защита фокуса; `<select multiple>` через `:selected`; `:key` по индексу; `url(...)` иконок через `cssIconUrl()` с allowlist схем; `messageBus` валидирует входящее сообщение.
- ⬜ **Секреты в argv** (`onecCommon`, `repositoryCommands` — `/P<pwd>`, `-Pwd`) видны в `ps` → файл параметров `/@`.
- ⬜ **MCP пишет по произвольным абсолютным путям** вне workspace (`V8McpServer` create_epf/erf/configRoot).
- ⬜ **Старт/UI-поток:** `Container.bootstrap` синхронно строит хеш-кэши в activate (отложить нельзя без риска — `reloadEntries` зависит от готовности кэшей; оставлено замечание в коде); полный ребилд DTO дерева на каждый refresh. Стоимость самого построения на ПОВТОРНЫХ активациях снята issue #43 (`FileStatIndex`, см. [architecture.md](./architecture.md#stat-индекс-рабочего-дерева-filestatindex)): без правок выгрузки `detect`/`ensureCaches` не читают содержимое файлов, только `statSync`; полное хеширование (~7 с на 59 503 файлах) остаётся ценой первого запуска/потери индекса. Сама синхронность `bootstrap` (выполнение в activate, а не в фоне) не изменилась — асинхронный старт по-прежнему вне рамок.

---

## P3 — Низкие / чистка

- ✅ **Мёртвый код src-ui (удалено 10 модулей):** `requestReply.ts`, `webviewState.ts`, `createSelectionStore.ts`, `createStatusStore.ts`, `utils/escape.ts`, `keyboard.ts`, `geometry.ts`, `assertNever.ts`, `types/command.ts`, `types/form.ts`. (`types/platform.ts` оказался живым — оставлен.)
- ✅ **Мёртвый код src:** удалены регистрация `compileExtensionToDb`, модуль `McpNodeRegistry` (+ его тест), `ConfigurationXmlEditor.isSamePath`, `RepositoryService.ensureScopeState`, `PropertiesViewController.getRenderTypeValue`.
- ✅ **Орфанное submenu** `v8vscedit.repositoryMenu` удалено из package.json.
- ✅ **Дедуп escape:** ~12 копий `escapeXml`/`escapeXmlText`/`escapeXmlAttribute` сведены к двум каноническим хелперам в `XmlUtils` (`escapeXmlText`, `escapeXmlAttribute`) без изменения вывода.
- ✅ **Дедуп:** `findExecutableInPath` объединён (OnecPlatform ↔ Standalone); скелет XML-парсера (`isTextNode`/`getElementName`/`getElementChildren`/`findFirstElement`) сведён в `XmlUtils`.
- ⬜ **Не объединено (расхождение поведения):** TCP-проверка порта (`TcpPortWaiter` vs `StandaloneServerService.canConnect` — разные таймауты/опрос); DTO-типы (`IconDto`/`TreeNodeDto` трижды).
- ⚠️ **Латентный баг:** две версии `collectText` разошлись (рекурсивная в `XmlUtils` vs не-рекурсивная в `ConfigXmlReader`) — НЕ унифицированы намеренно, требует решения «какое поведение верное для чтения состава».

---

## Оставшееся (открыто)
- **P2:** секреты в argv → файл параметров `/@`; ограничение MCP-записи путями workspace; асинхронный старт `bootstrap` (заблокировано зависимостью от кэшей); чанкование ребилда дерева.
- **P3:** дедуп TCP-проверки порта и DTO-типов (пропущены из-за расхождения поведения); унификация `collectText` (латентный баг — нужно решение).

---

## Журнал исправлений (2026-06-22)
Финальная чистая проверка (`rm -rf out && npm test`): `typecheck`, `lint`, `npm test` — зелёные, **359 passing, 0 падений**.

Закрыто (P0→P3):
- Весь кластер порчи данных XML (BOM/EOL, `$`-раскрытие, экранирование, nesting-aware, namespace-agnostic типы) + два краша.
- P0 #5 MCP mutation-guard; P1 #6 DNS-rebinding, #7 integrity, #8 таймаут, #9 кодировка, #10 гонка LSP, #11 TreeView, #12 reply/«Обзор», #13 readonly, #14 removeForm guard, #15 секреты→SecretStorage, #16 href-XSS, #17 XML-сущности.
- P2: надёжность процессов/ФС, утечки disposable, корректность парсинга/форм, Vue-баги контролов.
- P3: орфанное submenu, дедуп escape-функций/парсера/findExecutableInPath.
